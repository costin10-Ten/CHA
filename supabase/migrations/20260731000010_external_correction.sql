-- =============================================================================
-- Phase 8：外部 LLM 校正回填的審核動作
-- =============================================================================
-- 待選原子命題包丟到其他 LLM 交叉校正後回填時，必須留下與人工審核同等級的紀錄。
-- 回填不會核定任何原子命題：狀態一律維持待審核，只是把修正後的內容與理由寫進來。
--
-- 註：ALTER TYPE ... ADD VALUE 新增的值要等這個 migration 提交後才能使用，
-- 因此不在同一個檔案內引用它。

alter type public.review_action add value if not exists 'external_correction';
